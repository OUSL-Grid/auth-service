name: Tests - user and session repo functionality

on:
  push:
    branches: [main, develop, origin 3-design-oauth_identities-table]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Run tests user repo
        run: uv run pytest tests/test_user_repository.py

      - name: Run tests session repo
        run: uv run pytest tests/test_session_repository.py 

      - name: Run tests oauth identity repo
        run: uv run pytest tests/test_oauth_repository.py 